interface Shadow {
    main: string;
    hover: string;
}

export interface Shadows {
    card: Shadow;
    button: Shadow;
    buttonDark: Shadow;
    gray: Shadow;
    error: string;
    grayLight: string;
    menu: string;
}
