import styled from 'styled-components';

export const AccordionItemTitleStyled = styled.div`
  font-weight: 300;
  font-size: 21px;
  line-height: 25px;
  color: ${({theme}) => theme.palette.black};
`
